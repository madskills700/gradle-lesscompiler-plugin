buildscript {
    repositories {
        mavenLocal()
        mavenCentral()
    }
    dependencies {
        classpath('com.github.voplex95:gradle-lesscompiler-plugin:+')
    }
}

apply plugin: 'less'

lessCompile {
    source = file('input')
    target = file('output')
}